#include <stdio.h>
#include <stdlib.h>
#include "account.h"
#include "customer.h"
#include "csv.h"


int main() {

    Customer customer;
    constructCustomer(&customer);
    customer.id = 1;
    printf("%d", customer.id);
    displayCustomer(&customer);
    return 0;
}
